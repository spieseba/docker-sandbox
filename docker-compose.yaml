services:
  sandbox:
    container_name: sandbox
    image: docker-sandbox
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TZ: Europe/Berlin
    security_opt:
      - label=disable
    volumes:
      - ./workspace:/home/agent/workspace:rw
    stdin_open: true
    tty: true
    command: bash
